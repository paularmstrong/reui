# SelectField

import { Canvas } from '@site/src/components';
import { Example } from './example';

<Canvas>
	<div className="flex flex-col gap-5">
		<Example />
		<Example label="This is required" isRequired />
		<Example description="This is a help description because choosing is difficult." />
		<Example errorMessage="This is an error message because we all make mistakes." />
	</div>
</Canvas>
