# SelectField

import { Canvas } from '@site/src/components';
import { SelectFieldExample } from './examples';

<Canvas>
	<div className="flex flex-col gap-5">
		<SelectFieldExample />
		<SelectFieldExample label="This is required" isRequired />
		<SelectFieldExample description="This is a help description because choosing is difficult." />
		<SelectFieldExample errorMessage="This is an error message because we all make mistakes." />
	</div>
</Canvas>
